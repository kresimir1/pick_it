<h4>Edit a review </h4>

<%= form_for ([@product, @review]) do |f|  %>

  <p><%= f.label :title %></p>
  <p><%= f.text_field :title, size: 100 %></p>
  <p><%= f.label :content %></p>
  <p><%= f.text_area :content, size: "100x10" %></p>
  <p><%= f.submit %></p>
<% end %>
